$(document).ready(function() {
  /*
  $('.node dl.terminology').hide();
  $('.node dl.terminology').after('<div id="termPopup"><h2>POP!</h2></div>');
  
  
  $('.term-list li a').each(function(){
    // get term texts
    term = $(this).text();
    // remove spaces from texts
    termClass = term.replace(/ /g, '');
    // assign term classes
    $(this).addClass(termClass);
  });

  $('.term-list li a').bind('click', function() {
    $('.node dl.terminology').slideToggle();
    return false;
  });
*/
});